<!DOCTYPE html>
<html lang="en">
<head>
   <meta charset="UTF-8">
   <meta name="viewport" content="width=device-width, initial-scale=1.0">
   <meta http-equiv="X-UA-Compatible" content="ie=edge">
   <title>Today News</title>
   <link rel="stylesheet" href="/styles/main.css">
</head>
<body>
   <div class="container">
      <form action="/" method="POST" class="form__user-choice">
         <div class="form__options">
            <div class="form__categories">
               <legend class="form__title">Выбрать новости</legend>
               {{#each category}}
               <div class="form__category">
                  <input type="checkbox" name="{{this.categoryClass}}" id="{{this.categoryClass}}">
                  <label for="{{this.categoryClass}}">{{this.categoryName}}</label>
               </div>
               {{/each}}
            </div>
            <div class="form__quantity">
               <p class="form__title">Отображение</p>
               <p class="form__text">Кол-во новостей:</p>
               <select size="1" name="quantity">
                  {{#each quantity}}
                  <option value="{{this}}">{{this}}</option>
                  {{/each}}
               </select>
            </div>
         </div>
         <div class="form__buttons">
            <button type="submit" class="form__submit">Показать</button>
            <button type="reset" class="form__reset">Сбросить</button>
         </div>
      </form>
      <div class="news__block">
         {{#each allNews}}
            {{#each this.block}}
            <a class="news__card" target="blank" href="https://yandex.ru{{this.href}}">
               <h3 class="news__title">{{this.title}}</h3>
               <span class="news__category {{this.categoryClass}}">{{this.categoryName}}</span>
               <img src="/img/link.svg" alt="В новом окне" class="news__icon">
            </a>
            {{/each}}
         {{/each}}
      </div>

   </div>
</body>
</html>